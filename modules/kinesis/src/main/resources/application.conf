{
  "input": ${snowplow.defaults.sources.kinesis}
  "input": {
    "appName": "snowplow-databricks-loader"
  }
  "output": {
    "bad": ${snowplow.defaults.sinks.kinesis}
  }
  "streams": ${snowplow.defaults.factories.kinesis}
}
